query RunInput {
  cart {
    lines {
      merchandise {
        __typename
        ... on ProductVariant {
          product {
            cisCollectLocations: metafield(namespace: "custom", key: "collect_locations") {
              value
            }
          }
        }
      }
    }
    deliveryGroups {
      id
      deliveryOptions {
        handle
        title
      }
    }
  }
}
